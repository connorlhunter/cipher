[package]
name = "cipher-native-transport"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
cipher-realtime-protocol = { path = "../cipher-realtime-protocol", version = "^0.1.0-prealpha.10" }
cipher-types = { path = "../cipher-types", version = "^0.1.0-prealpha.10" }
serde.workspace = true
serde_json.workspace = true
zeroize = "^1.8.2"

[dev-dependencies]
native-tls = "^0.2.14"
tokio = { workspace = true, features = ["io-util", "time"] }
tokio-native-tls = "^0.3.1"
tokio-tungstenite = { version = "^0.29.0", default-features = false, features = ["connect", "native-tls"] }
